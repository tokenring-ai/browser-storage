{
  "name": "@tokenring-ai/browser-storage",
  "description": "Provides browser-based agent state storage for the TokenRing ecosystem using localStorage for persistent checkpoint management.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/checkpoint": "0.2.0",
    "zod": "^4.3.6",
    "uuid": "^13.0.0"
  },
  "license": "MIT",
  "main": "index.ts",
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  }
}
