{
  "name": "@tokenring-ai/browser-storage",
  "description": "Provides browser-based storage for the TokenRing ecosystem.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/checkpoint": "0.2.0",
    "zod": "^4.3.6",
    "uuid": "^13.0.0"
  },
  "license": "MIT",
  "main": "index.ts",
  "type": "module",
  "devDependencies": {
    "vitest": "^4.0.18",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  }
}
